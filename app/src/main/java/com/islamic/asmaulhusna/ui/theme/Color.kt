package com.islamic.asmaulhusna.ui.theme

import androidx.compose.ui.graphics.Color

// ── Mushaf — "the illuminated page" ──────────────────────────────
// A single, committed nightfall palette: deep emerald grounds, gold-leaf
// ornament, warm cream text. No light mode, by design — like a hand-gilded
// mushaf viewed by lamplight.

val Ink        = Color(0xFF05130F) // deepest ground
val Ink2       = Color(0xFF081D16) // panel ground
val Page       = Color(0xFF0A2019) // screen background
val Emerald    = Color(0xFF0C3A2C) // card ground
val EmeraldLo  = Color(0xFF0A241B) // card ground, lower gradient stop
val EmeraldHi  = Color(0xFF123F30) // card ground, upper gradient stop
val EmeraldGlow= Color(0xFF17553F) // lifted emerald — luminous top of hero/today cards
val EmeraldRow = Color(0xFF0E3125) // grid cell ground
val EmeraldLine= Color(0xFF17392C) // hairline separators on emerald
val SectGround = Color(0xFF0B271E) // section-card ground

val Gold       = Color(0xFFD4AF37) // gold leaf — primary accent
val GoldSoft   = Color(0xFFE8CF7E) // lighter gold, for Arabic/transliteration
val GoldDim    = Color(0xFF9C8235) // muted gold, for hairlines & numerals
val GoldInk    = Color(0xFF231A05) // near-black ink for text on gold

val Cream      = Color(0xFFF3EAD2) // primary reading colour
val CreamDim   = Color(0xFFC9BD97) // secondary/caption text

// gold at ~24% — ornamental double-rule frames
val GoldLine   = Color(0x3DD4AF37)
